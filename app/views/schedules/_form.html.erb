<%= form_with(model: schedule) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
        <% schedule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :date_open %>
    <%= form.date_select :date_open, {order: [:day, :month, :year]}, {class: "form-control", style:"display:inline-block; width:30%; margin:auto;"} %>
  </div>

  <div class="form-group">
    <%= form.label :date_close %>
     <%= form.date_select :date_close, {order: [:day, :month, :year]}, {class: "form-control", style:"display:inline-block; width:30%; margin:auto;"} %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
